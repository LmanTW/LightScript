import { relevance, keywordSyntax } from './Syntax.json' assert { type: 'JSON' }

export {  }

//檢查關聯性
function checkRelevance (codeSegment) {
  for (let item of codeSegment) {
    if (item.type === '')
  }
}

//檢查關鍵字的語法
function checkRelevance (codeSegment) {
}